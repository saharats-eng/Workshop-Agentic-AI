const history=[];const list=document.querySelector('#messages');const form=document.querySelector('#form');const input=document.querySelector('#message');
function add(role,text){const el=document.createElement('div');el.className=`bubble ${role}`;el.textContent=text;list.append(el);list.scrollTop=list.scrollHeight}
form.addEventListener('submit',async(e)=>{e.preventDefault();const message=input.value.trim();if(!message)return;const provider=document.querySelector('#provider').value;const model=document.querySelector('#model').value.trim();add('user',message);history.push({role:'user',content:message});input.value='';try{const r=await fetch('/api/chat',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({message,history:history.slice(0,-1),provider,model:model||undefined})});const data=await r.json();const reply=data.reply||data.error||'เกิดข้อผิดพลาด';add('assistant',reply);history.push({role:'assistant',content:reply})}catch{add('assistant','เชื่อมต่อ backend ไม่สำเร็จ กรุณาลองใหม่')}});